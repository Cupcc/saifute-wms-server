---
description: 当 agent 停止时发送飞书 webhook 通知用户
alwaysApply: true
---

# Agent / Subagent 飞书通知

用户会在下面这些时机收到飞书消息。除了主代理停止并交回用户外，**当 orchestration main agent 启动子代理干活时，也需要发送开始提醒；子代理完成其委派工作后，也需要发送结束提醒。**

默认由 **orchestration main agent 统一负责发送**，避免父子代理重复通知；如果某个子代理被明确设计为自行发送，则父代理必须避免重复发送同一条消息。

## 用户何时会收到消息

| 场景 | 谁负责发送 | 推荐 event | 何时发送 | 是否带运行时长 | 用户会收到什么 |
| --- | --- | --- | --- | --- | --- |
| 启动一个子代理开始干活 | 默认 `main agent` | `subagent_start` | 子代理已启动、主代理准备等待其结果时立即发送 | 否 | 告知用户哪个子代理开始处理哪类工作，下一步会等待什么结果 |
| 一个子代理完成了被委派的工作，但主代理还会继续编排 | 默认 `main agent` | `subagent_complete` | 主代理收到子代理 handoff 后立即发送 | **是，需显式传入 `--duration-ms` 或 `--started-at-ms`** | 告知用户哪个子代理已完成、产出了什么、本次子代理运行时长、主代理接下来继续做什么 |
| 主代理本轮任务已完成，并把控制权交回用户 | `main agent` | `task_complete` 或 `complete` | **最终回复用户前**发送一次 | 是，默认附加 `session` 运行时长 | 告知用户核心结果、下一步计划，并自动追加本次任务运行时长 |
| 主代理暂停、阻塞、等待确认，但本轮要停下来交回用户 | `main agent` | `need_user_input`、`agent_stop` 或 `phase_complete` | **最终回复用户前**发送一次 | 否 | 告知当前状态、阻塞项或待确定项，以及用户回复后下一步怎么继续 |

## 执行方式

在需要通知的节点执行一次：

```bash
pnpm notify:feishu "事件名" "消息内容" "消息类型"
pnpm notify:feishu "subagent_start" "已启动 planner 子代理；下一步计划：等待规划结果并继续编排。" "info"
pnpm notify:feishu "subagent_complete" "planner 子代理已完成并返回执行建议；下一步计划：开始按计划实施。" "info" --duration-ms <子代理启动到结束的精确毫秒数>
pnpm notify:feishu "task_complete" "核心结果；下一步计划：..." "success"
pnpm notify:feishu "task_complete" "核心结果；下一步计划：..." "success" --duration-ms <精确毫秒数>
```

**参数说明**：

- **事件名 (event)**：简短标识，如 `subagent_start`、`subagent_complete`、`task_complete`、`agent_stop`、`need_user_input`、`phase_complete`。
- **消息内容 (msg)**：给用户看的说明，默认需要写清**核心结果**和**下一步计划**。
  - 对 `task_complete`（或你明确使用的最终完成事件 `complete`），`msg` 中**不要手写运行时间，更不能写"约 12 分钟"这类估计值**。优先依赖 `sessionStart` hook 写入的会话级开始时间，由脚本自动追加类似 `本次任务运行：1小时23分钟` 的自然语言时长；若会话级状态不可用，再显式传 `--duration-ms` 或 `--started-at-ms`。
  - 对 `subagent_complete`，`msg` 中**不要手写运行时间**；必须通过 `--duration-ms` 或 `--started-at-ms` 显式传入子代理运行时长，脚本会自动追加 `本次子代理运行：...`。缺少精确计时输入时脚本会在发送 webhook 前终止并报错。
  - 对其他事件，包括 `subagent_start`、`phase_complete`、`need_user_input`、`agent_stop`，`msg` 中**不要包含运行时间**，只保留核心结果和下一步计划。
  - 完成事件推荐格式：`<核心结果>；下一步计划：<后续动作>`，由脚本自动补充时长
  - 其他事件推荐格式：`<核心结果>；下一步计划：<后续动作>`
- **消息类型 (type)**：如 `info`、`success`、`warning`。

**示例**：

- 子代理开始：`pnpm notify:feishu "subagent_start" "已启动 code-reviewer 子代理审查本轮变更；下一步计划：等待审查结果并处理问题。" "info"`
- 子代理结束：`pnpm notify:feishu "subagent_complete" "code-reviewer 子代理已完成审查并返回结论；下一步计划：根据 findings 继续修复或收尾。" "info" --duration-ms 180000`
- 任务完成：`pnpm notify:feishu "task_complete" "当前任务已完成，请查看变更与总结；下一步计划：请确认结果，若无问题我将进入下一批迁移。" "success"`
- 需要用户：`pnpm notify:feishu "need_user_input" "遇到需您确认的选项；下一步计划：请回复确认项，我收到后继续执行。" "info"`
- 仅阶段结束：`pnpm notify:feishu "phase_complete" "Review 阶段结束，无 blocking 问题；下一步计划：我将继续整理修复项并推进下一阶段。" "info"`

**计时要求**：

- `task_complete` / `complete` 通过 `sessionStart` hook 维护的会话级运行时状态自动计时，直接调用 `pnpm notify:feishu ...` 即可。完成事件不会自动退回到 task 级状态；若会话级状态不可用，再显式传 `--started-at-ms <开始时间戳>` 或 `--duration-ms <毫秒数>`。
- `subagent_complete` 不读取 session/task hook 状态，必须由调用方通过 `--duration-ms <子代理耗时毫秒数>` 或 `--started-at-ms <子代理启动时间戳毫秒数>` 显式提供；缺少精确计时输入时脚本报错退出，不发送 webhook。
- 通知里展示给用户的是自然语言时长，例如 `本次任务运行：1小时23分钟` 或 `本次子代理运行：3分钟`；不要展示时间戳，也不要使用"约""大约""预计"等估计措辞。
- `--started-at-ms` 与会话状态里的 `startedAtMs` 必须为**毫秒**级 Unix 时间。若误写成**秒**（数值约 `1e9`），脚本会按约定自动乘以 1000；若仍出现极端时长，请核对 hook 与调用方是否混用秒/毫秒。

**审计与 task_id**：

- 每次通过 `pnpm notify:feishu` 发送时，会在项目根目录 `logs/feishu-notify.log` 追加 JSON 行（含 `phase`：`send_attempt` / `send_ok` / `send_error`），便于本地排查。
- 发送给飞书的 `msg` 末尾会自动追加 `；task_id：<id>`，优先级：`FEISHU_NOTIFY_TASK_ID` → `current-task.json` 的 `generationId` → `current-task.json` 的 `conversationId` → `current-session.json` 的 `conversationId`（仅会话 hook 写过、任务状态缺失时仍可关联）。`task_start` 类通知由 `task-start-notify` hook 使用 `generation_id` 同样追加并写同一日志文件。

## 额外约束

- 不需要为子代理内部的每一个细小步骤都发消息，只在"开始干活"和"结束交付 handoff"两个边界发。
- 同一件事只发一条通知；如果父代理已经代表子代理发送开始或结束通知，子代理不要再重复发送。
- `task_complete` / `complete` 是主代理本轮真正收尾时使用的完成事件；不要把子代理结束误报成整轮任务完成。
- `subagent_complete` 的运行时长只代表该子代理本次工作的时长，与整个 session 运行时长语义不同；两者使用不同标签（`本次子代理运行` vs `本次任务运行`），不得混用。
